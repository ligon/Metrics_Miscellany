#+TITLE: Agent Operations Guide
#+AUTHOR: Metrics Miscellany Maintainers
#+OPTIONS: toc:nil num:nil

* Purpose
This guide spells out the expectations for automated assistants (LLMs, bots, scripts)
working on *Metrics_Miscellany*.  It supplements human-oriented guidance in
=README.org= and =CONTRIBUTING.org= by turning them into checklists that agents can
execute repeatably.

* Scope
- Follow the coding and documentation conventions in [[file:docs/index.org][docs/index.org]] and
  [[file:CONTRIBUTING.org][CONTRIBUTING.org]].
- Human contributors still review all substantive changes; this document only clarifies
  how agents prepare that work.
- Use =docs/automation/= for run logs or follow-up notes that should persist beyond a
  single session.

* Operating Principles
1. **Preserve semantics** – retain existing estimator APIs and notation when editing
   the modules under =metrics_miscellany/=.
2. **Minimal footprint** – modify only the files required for the task; avoid bulk
   formatting or refactors without explicit approval.
3. **Verifiable output** – accompany code changes with reproducible commands (tests,
   linting) that maintainers can replay via Poetry.
4. **Transparent reasoning** – document non-obvious design choices in commit messages or
   short Org notes under =docs/automation/=.  Include file references when possible.
5. **Explicit attribution** – add a =Co-authored-by: Assistant (Codex CLI)= trailer (or the
   correct agent tag) to commits produced by automations so history stays clear.
6. **ASCII-safe math** – when editing Org documents, prefer LaTeX commands like
   \mu, \Sigma, and display delimiters \[ ... \]; avoid literal Unicode math glyphs.

* Pre-Execution Checklist
1. Sync the working tree with the target branch (typically =master= or a dedicated feature
   branch) without rewriting history.
2. Review [[file:CONTRIBUTING.org][CONTRIBUTING.org]] and this guide to confirm tooling expectations.
3. Inspect task-specific instructions in =README.org= or issue notes.
4. Confirm sandbox limits (filesystem scope, network policy).  Request maintainer approval
   before running commands that need elevated permissions or network access.

* Tooling & Commands
- Use Poetry for everything: =poetry install=, =poetry run ...=, =poetry build=.
- Prefer Make targets over ad-hoc invocations:
  - =make quick-check= for the default lint/type/test sweep.
  - =make slow-tests= for marked slow cases.
  - =make coverage= for a coverage report with both terminal-missing
    and HTML output under =htmlcov/=.  Settings live in
    =[tool.coverage.*]= in =pyproject.toml=; tests are omitted so the
    headline number reflects production-code coverage only.
  - =make use-local-datamat= to override the virtualenv with the sibling =DataMat= checkout
    during tandem development; run =poetry install --sync= when finished.
- Stick to fast search utilities (=rg=, =python - <<'PY'= scripts) for inspection; avoid
  destructive commands like =rm -rf= or =git reset --hard=.

* Source layout (operational notes)
- The Python modules under =metrics_miscellany/= are the source; edit them
  directly.  Through 0.3.x they were tangled from a literate Org file,
  =metrics_miscellany.org=, and the rule was the opposite -- never hand-edit a
  .py.  That source was retired in 0.4.0; see [[file:docs/reorg-0.4.0.org][docs/reorg-0.4.0.org]].  To
  consult the retired original, =git show d63c113:metrics_miscellany.org=.
- Two consequences of that history are still visible in the tree and are being
  unwound in 0.4.0: =utils.py= and =tests.py= carry imports scattered through
  the file, a residue of concatenated Org blocks that each re-imported numpy
  and pandas; and =[tool.ruff.lint.per-file-ignores]= in =pyproject.toml=
  suppresses I001, E402 and F811 across the package to tolerate them.  Do not
  treat either as intentional style.

* Collaboration Protocol
- **Task intake**: restate the assignment, list target files, flag ambiguities before editing.
- **Status updates**: maintain a short status section in any active automation log.
- **Timestamps**: use active Org timestamps (e.g., =<2025-10-27 Mon>=) in logs to record
  when actions occurred.
- **Handoff**: summarize edits, link to files (`path:line`), and list verification commands.
- **Review support**: provide focused diffs or highlights; do not auto-format untouched files.
- **Quality checks**: unless explicitly excused, run:
  1. =poetry run ruff check .=
  2. =poetry run black --check .=
  3. =poetry run mypy metrics_miscellany test= (after configuration)
  4. =poetry run pytest=

* Incident Response
- If a command fails (permissions, sandbox, missing dependency), stop and document the
  failure along with the proposed workaround before retrying.
- When encountering unexpected repository state (partial builds, a dirty tree you did
  not create), pause and request maintainer guidance rather than overwriting files.
- Log any manual rollbacks or file restores in =docs/automation/status_log.org= (or a new
  dated entry).

* Change Log Template
Use the format below when updating this guide or other automation policies:
#+begin_example
** <YYYY-MM-DD Day> Agent/Author Tag
- Summary of changes and affected sections
- Commands or policies updated
#+end_example

* Current Change Log
** <2025-10-27 Mon> Assistant (Codex CLI)
- Initial version tailored to the Metrics_Miscellany workflow.
- Captured Poetry-based tooling, quick-check expectations, and attribution rules for agents.

** <2026-05-26 Tue> Sue (Claude Opus 4.7, 1M context)
- Code-review pass on multiindex handling and miscellaneous estimator
  routines.  Eleven commits landed; suite went from 28 to 73 tests, all
  passing.  Production-only coverage went from untracked to 79.3%
  (=order_statistics.py= 20% -> 100%; =utils.py= 47% -> 73.8%;
  =estimators.py= 66% -> 85.9%).
- Substantive bug fixes (each documented in its commit):
  - =dummies= MultiIndex level names were hash-seed-dependent (set-based
    partitioning); now order-preserving.
  - =random.permutation= / =randomization_inference= "permute within
    fixed levels" idiom was a no-op shuffle; now actually permutes.
  - =linear_gmm= recursive call dropped =return_Omega=.
  - =restricted_tsls= R-supplied branch crashed before the solver and
    referenced an undefined =lm= on return; rewritten around a NumPy
    saddle-point solve.
  - =cragg_donald= chi-square df was =n - m + 1= (scaled with sample
    size); now =l - m + 1=, the correct asymptotic.  Empirically
    verified: uniform p-values under H_0 across multiple (n, m, l).
  - =chi2_test= raised =ValueError: matrices are not aligned= when
    =var_selection= and =R= were both supplied; now auto-restricts R
    to the selected subset.
  - =utils.diag(series, sparse=True)= returned NaN off-diagonal; now
    explicit fill_value=0 SparseDtype while preserving sparse memory.
  - =utils.sqrtm= could not detect indefinite input (guard inspected
    singular values, which are non-negative); rewritten around
    =eigh= with a tolerance window for FP noise.
  - =kron= silently mishandled flat-Index operands by character-
    unpacking string labels; reworked to promote labels through
    =MultiIndex.from_tuples= safely.
- Test-hygiene cleanups: three former script-style test files
  (=test_kr79.py=, =test_randomization_inference.py=, =test_fwl_regression.py=)
  ran their bodies at every pytest collection but defined no
  =test_*= functions; converted to proper seeded tests or removed.
- New infrastructure: =pytest-cov= dev dep, =[tool.coverage.*]= in
  =pyproject.toml=, =make coverage= target, coverage artifacts
  added to =.gitignore=.
- Outstanding follow-ups for future sessions:
  - =GMM_class= (OO wrapper around =gmm= procedural module) has 27%
    coverage; no test exercises =GMM.__init__= or its methods.
  - =gmm.continuously_updated_gmm=, =gmm.dgN=, =gmm.Vb= are untested.
  - =kernel_methods.rectangular=, =kernel_methods.gram= DataMat
    branch are untested.
  - =utils.kron='s sparse path and ndarray-x-ndarray case lack tests;
    several =drop_vestigial_levels= branches likewise.
  - =factor_analysis= still has only the =svd_method='eig'= branch
    working; =lapack=/=randomized= remain =NotImplementedError= stubs.
  - Procedural =gmm= module mutates module-attribute state
    (=gmm.gj = gj=) -- two =GMM= instances clobber each other.  Worth
    deprecating in favor of the class API once that has coverage.
  - =DataMat.dg()= (datamat package, not this repo) carries the same
    sparse-fill-value-NaN issue that =utils.diag= just fixed.
    =kernel_methods.kernel_regression_variance= now sidesteps it by
    using NumPy's =diag= directly; flag if you touch other DataMat-
    based code that calls =.dg().dg()=.
